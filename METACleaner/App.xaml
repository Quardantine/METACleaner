<Application x:Class="METACleaner.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:METACleaner"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <Style x:Key="Button_Top_Side" TargetType="Button">
            <Setter Value="Transparent" Property="Background"/>
            <Setter Value="#939393" Property="Foreground"/>
            <Setter Value="12" Property="FontSize"/>
            <Setter Value="0" Property="BorderThickness"/>
            <Setter Value="Center" Property="HorizontalAlignment"/>
            <Setter Value="Center" Property="VerticalAlignment"/>
            <Setter Value="0" Property="Margin"/>
            <Setter Value="Transparent" Property="BorderBrush"/>
            <Setter Value="25" Property="Height"/>
            <Setter Value="25" Property="Width"/>
            <Setter Value="False" Property="Focusable"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" Padding="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Value="White" Property="Foreground"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">

                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">

                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Application.Resources>
</Application>
